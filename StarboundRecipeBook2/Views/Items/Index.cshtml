
@{ ViewData["Title"] = "Items"; }
@model List<Item>

@foreach (var item in Model)
{
	if (item.Icon != null)
	{
		string base64 = Convert.ToBase64String(item.Icon);
		<img src=@String.Format("data:image/jpg;base64,{0}", base64) />
	}
}
